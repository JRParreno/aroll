# Copy to `local.properties` (same folder). That file is git-ignored.
#
# Flutter/Android SDK paths are usually written automatically by the IDE:
#   sdk.dir=...
#   flutter.sdk=...
#
# Maps use OpenStreetMap via flutter_map — no Google Maps API key is required.
